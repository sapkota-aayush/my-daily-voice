[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd realtime-agents && npm ci"]

[phases.build]
cmds = ["cd realtime-agents && npm run build"]

[start]
cmd = "cd realtime-agents && npm start"

